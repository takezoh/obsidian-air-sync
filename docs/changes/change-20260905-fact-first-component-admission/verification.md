---
change: change-20260905-fact-first-component-admission
role: verification
---

# Verification

Design status: the user approved the responsibility-contract corrections and the ADR is accepted. Local implementation, the complete repository command gate and two independent code reviews passed; persistent-design promotion is applied. The user subsequently reported successful actual-vault testing, corroborated by the bounded log inspection below. Deployment was not performed by the agent. See the [design acceptance and command matrix](design-plan/design.md#acceptance) for the broader acceptance contract.

## Actual-vault observation (2026-09-05)

The user reported that operation was normal. The deployed plugin and the reviewed repository build had identical SHA-256 `2e13d050513a89cec90cbeed1a24ef528b542d3bef7df04288b1ce68b3b33989`. Inspection of `.airsync/logs/<device>/2026-09-05.log` covered the latest reload at 11:11:01.885 UTC through 11:23:13.660 UTC: 16 started cycles and 16 clean completions, with zero warnings, errors, failed actions, blocked actions or conflicts. Acquisition comprised 10 WARM, 3 COLD and 3 HOT cycles; seven plans were empty. Case-only folder/file renames and rename-plus-edit completed, followed by no-action convergence.

Two state-clear events were also present; their actor is not established by these logs. Rename cycles had already succeeded before those events, so this is not evidence that reset was required. The subsequent full scan completed with 2,951 match actions and no failures in approximately 4 minutes 28 seconds. Historical alias warnings preceded the latest reload and did not recur in the inspected interval. This supports the user's reported result for this run, not every concurrent-conflict or fault-injection scenario; those remain separately covered by the automated tests. Raw personal vault paths and filenames are intentionally omitted.

## Latest verification (2026-09-05, final reviewed code)

All four required commands passed with exit code 0 on the same reviewed source tree: `npm run lint`, `npm run lint:bot-repro`, `npm run build`, and `npm run test:coverage`. **1,862 tests / 90 files** and **55 structural/reproduction tests** passed, with no skipped/todo tests. Coverage: statements 84.38%, branches 80.36%, functions 83.02%, lines 85.93%. Session-local logs are `/tmp/air-sync-gate-final-{lint,bot,build,coverage}.log`. No real filesystem/provider acceptance is inferred from these fake-backed tests.

Both independent reviewers approved the final source changes with no findings after correcting hashless transfer rejection, unaccounted source reports, ordinary-conflict preservation, arriving rotation targets and case-alias copy addressing. Their canonical final verdicts are `/tmp/air-sync-review-correctness-final.json` and `/tmp/air-sync-review-failures-final.json`.

- `fact-first-execution.test.ts` now has 58 public Admission→Executor→publication cases, including same-path preservation-write lies, source change during preservation, copy corruption during target effects, arriving foreign destination protection, hash-empty/opaque-checksum transfer, unrelated target baseline, symmetric old-name recreation, and ordinary duplicate/merge interruption followed by re-observation to a fixed point.
- `conflict.test.ts` and `conflict-resolver.test.ts` test the sole resolver's selected bytes and verified copies without original-path mutation. Original effects and partial-merge behavior moved to the public execution suite. The obsolete low-level compensating rollback assertion is replaced by ordinary interruption/convergence coverage; successful merge/newer-wins does not allocate unnecessary copies, and invalid JSON retains duplicate fallback.
- The existing cold-scan mixed-child case-only folder integration was not weakened. It caught an incorrect requirement that a preservation address equal the adapter-resolved spelling. Storage-byte proof now respects that boundary; original exact endpoint and parent terminal proofs remain enforced. Orchestrator plus public execution tests pass 153 cases.
- Concurrency tests await the delete-start signal rather than a fixed delay. The serialization/max-concurrency assertions are unchanged; no timeout increase, retry or sleep was added.
- Mutation run `preservation-terminal`: removing terminal output coverage made the target-write corruption case fail; restoration passed (`/tmp/air-sync-mutation-preservation-{red,green}.log`). Mutation run `source-accounting`: accepting absent source records unconditionally made the unrelated target-record case fail; restoration passed (`/tmp/air-sync-mutation-source-accounting-{red,green}.log`).
- Exact-HEAD scope evidence passes both out-of-scope and evidence-readiness queries (`/tmp/air-sync-reviewed-scope.json`). This is not a declaration that the change may be closed.

The raw fake-green scanner reports 17 findings for removed optimizer suites and changed assertion strings/counts across the approved contract migration, with no skip/only/todo additions (`/tmp/air-sync-fake-green-guard.json`). This raw result is **not green** and is not rewritten. Independent test-discipline review approved their semantic disposition with no findings: the comparer/evidence/Admission assertions move from proposed actions to current facts; exact publication and supersession assertions now require real receipts; resolver original-path effects and rollback move to public executor interruption tests; the deleted optimizer suites map to the current public safety scenarios listed in implementation.md. OneDrive's redundant-report case now checks equivalent current-fact decisions. No missing safety property or unjustified weakening was found in that bounded audit. This reviewed classification is separate from the scanner's literal violation result; no obsolete API was restored to manufacture a mechanical pass.

Persistent-design promotion was applied after the user approved the one-time direct registration exception. The manifest contains explicit stable-ID operations and their required verification references; the docs CLI validated them, transitioned the change to `closing`, and applied the promotion to `design-four-stage-sync-pipeline`. Its prose and source references now agree with the promoted rules. Lifecycle and promotion application were not patched directly. Subsequent user testing and matching-build log evidence are recorded above; PR integration and package closure are separate operations, and the change is not `done`.

## Historical pre-review verification (2026-09-05 18:44 UTC)

The complete command chain `npm run lint && npm run lint:bot-repro && npm run build && npm run test:coverage` passed on the pre-review code tree: **1,840 tests / 90 files**, plus **55 structural/reproduction tests**. Coverage: statements 84.18%, branches 79.61%, functions 83%, lines 85.69%. Session-local output: `/tmp/air-sync-gate-coverage-3.log`. Later documentation-only changes do not constitute a new code verification run. Independent failure-handling review returned approved/no findings; independent correctness review is still running. No deployment, commit or actual-vault acceptance was performed.

- Old Admission and Executor contract migrations are now green, not the historical 143 failures recorded below. Complete current observations preserve unknown/mismatching identity, vacancy, scope, suffix and kind counterexamples.
- Public execution covers cross-hidden-boundary folder moves as supported per-file moves, interrupted movement and interrupted exact publication followed by normal observation to an empty plan (`/tmp/air-sync-cross-boundary-execution.log`). No recovery branch or marker is an input.
- Same-path conflict publication with a vanished terminal endpoint failed twice before common terminal verification (`/tmp/air-sync-ordinary-conflict-red.log`); both cases now block without a SyncRecord while preserving the remote conflict copy. Push/pull source-disappearance and contradictory read-buffer witnesses likewise passed after the shared transfer proof repair.
- Committed identity evidence associated with a different observation address failed before recording the actual SyncRecord key (`/tmp/air-sync-record-evidence-red.log`) and passes afterward (`/tmp/air-sync-record-evidence-green.log`).
- Folder descendant deletion tests cover both move directions and both old/new baseline key locations. Linear 512-child receipt lookup failed before replacement (`/tmp/air-sync-receipt-linear-red.log`) and passes without another receipt collection.
- Retired optimizer tests are replaced by current public-contract scenarios listed in implementation.md. No skipped/todo test or lowered coverage threshold was used. Removed production modules are recoverable from repository history; no vault files or stored sync data were deleted.

## Historical partial implementation evidence (2026-09-05)

Discriminating failing runs were recorded before implementation in `/tmp/air-sync-publication-red.log`, `/tmp/air-sync-folder-publication-red.log`, `/tmp/air-sync-content-equality-red.log`, `/tmp/air-sync-scope-capture-red.log`, `/tmp/air-sync-component-order-red.log`, `/tmp/air-sync-tracker-generation-red.log`, and `/tmp/air-sync-merge-base-bytes-red.log`. These are session-local logs, not durable acceptance artifacts. The corresponding source tests preserve the witnesses.

Store/Executor focused verification passed 106 tests after adding exact publication and priority drain/exclusion witnesses. After adding tracker ABA and byte-bound merge-base tests, committer/Executor/tracker focused verification passed 133 tests. Scope/planning/change-detector verification previously passed 97 tests. These results do not establish the missing fact-first Admission, complete publication footprint, generic rename protocol, or sole finalizer contract.

The last full code gate **before the central replacement** passed lint, bot reproduction and build; coverage reported **1807 passed / 2 failed** across 91 files (`/tmp/air-sync-implementation-coverage.log`, 2026-09-05 14:58 UTC). This is historical evidence, not a result for the current tree.

Continuation verification (session-local logs, not final acceptance):

- Public observation witnesses for actionless aliases, missing-baseline publication and remote rename+edit failed first (`/tmp/air-sync-public-admission-red.log`) and passed after fact-first binding.
- `fact-first-execution.test.ts` exercises Admission → real executor → store fake without proposed-action input. It covers remote rename+edit, successful I/O followed by CAS failure and normal re-observation, and request-echo blocking followed by provider confirmation. Removing the moved-endpoint binding made the CAS replay test fail (`/tmp/air-sync-relocation-mutation-red.log`); restoring it passed.
- Finalization tests cover incomplete/missing exact action success, record-relocation proof, checkpoint failure, synchronous/asynchronous attempt exceptions, sibling settlement before abort, and no repeated abort on abort failure. Notification tests forbid an incomplete actionless cycle from displaying up-to-date.
- Orchestrator's 95 integration tests and the three new public execution tests passed together. Positive scenarios explicitly supply provider-confirmed write metadata; the canonical remote mock still defaults to request echoes. The case-insensitive folder fixture now models alias reads as well as alias writes/stat, so preservation readback exercises the same filesystem behavior.
- After removing the old fresh-state executor and unsafe committer fallback, committer/public-execution/Orchestrator verification passed 122 tests (`/tmp/air-sync-exact-publication-verification.log`). Public-input migrations of crash/delete/priority and OneDrive delta integration fixtures passed 24 tests.
- **Unfinished migration remains RED:** the old Admission/Executor fixture suites reported 143 failed / 27 passed (`/tmp/air-sync-legacy-tests-migration-baseline.log`). Their action-input API and incomplete execution snapshots are not accepted final coverage. Counterexamples must be retained as complete public facts or explicit executor-boundary fixtures; do not skip these tests or weaken safety to obtain a green result.

Latest continuation evidence (2026-09-05 17:15 UTC; session-local logs):

- Parent terminal proof first missed child content and identity races (`/tmp/air-sync-parent-proof-red.log`: two failures, unchanged control passed). The public pipeline now tests unchanged, content-changed, identity-substituted, deleted-child-recreated, and preservation-copy-tampered outcomes. The content-race test continues through normal re-observation/pull to a no-action fixed point with no second parent rename or recovery state.
- Cross-algorithm copy performed a redundant destination read (`/tmp/air-sync-copy-reuse-red.log`). Reusing its already captured transfer buffer passes the exact one-source-read/zero-destination-read assertion. Committed-fingerprint pure rename and unchanged parent proof require zero explicit filesystem content reads; parent stat counts are pinned per descendant.
- Same-address file/directory collisions formerly returned resolved_no_action (`/tmp/air-sync-kind-collision-red.log`). Both orientations now fail Admission without effects.
- Real composition tests exposed missing content acquisition for remote rename sources and unnecessary unchanged-child match publications. `change-detector`, `convergence`, `orchestrator` and public execution tests passed 200 cases after repairing these boundaries (`/tmp/air-sync-rename-acquisition-green.log`). Convergence fixtures now assert successful execution and clean finalization before acknowledgment rather than acknowledging failures.
- A separate RED witness caught attaching an earlier local hash to newer matching remote bytes (`/tmp/air-sync-enrichment-version-red.log`). Buffer/hash/version consistency is now checked; all 74 change-detector tests pass. Old positive fake hash literals were replaced with the actual SHA-256 of their fixture bytes, not weaker assertions.
- The Store ownership scanner previously missed destructured references and three CAS method names. Ten synthetic mutation fixtures failed before scanner repair. The same closed owner inventory now passes without adding or removing allowed owners. Bot reproduction passes 52 tests and all three dependency-declaration boundaries.
- Latest exact command chain `npm run lint && npm run lint:bot-repro && npm run build && npm run test:coverage` passed its first three commands, then coverage failed: **1716 passed / 143 failed**, 90 files passed / 2 failed (`/tmp/air-sync-latest-coverage.log`). Remaining failures are Admission (77 of 101) and Executor (66 of 71). This is not a successful delivery gate or a coverage-floor acceptance. Old API removal, faithful fixture migration, exhaustive boundary/performance verification, and independent implementation review remain required.

Documentation lint with conformance and `git diff --check` pass. The ownership-guard scope entry records a path already present in the approved implementation effect descriptor; it does not introduce another permitted writer. No actual-vault replay, deployment, independent implementation approval, or completion claim is supported.

The initial document conformance check during implementation found empty outcome/classification fields in the active manifest. Those declarations were populated from the already approved contract, not by changing its requirements. The scope declaration now explicitly includes the shared store mock and OneDrive's dependent ScopeProjection fixture. The subsequent document lint with conformance returned `ok`, 44 indexed documents, no warnings. `git diff --check` also passed. An evidence query returned `UNKNOWN(stale)`; this is not a scope-conformance PASS. Read-only Git diff inspection found the changed production/test files confined to the declared sync boundary and these two supporting fixture paths.

## Historical design evidence

Historical design review on 2026-09-05 initially returned approved, but that approval was withdrawn after three P1 contract findings: flat phases can reorder dependent B/A actions, same-identity-only destination policy blocks authorized foreign replacement/replay, and filtered included facts alone cannot distinguish scope-incompatible re-derived relations. The earlier approval does not apply to the revised design. Existing physical-exclusion overreach was correctly removed and inclusion XOR/both-excluded policy remains preserved.

The revised contracts require ordered existing component actions with per-action publication/prefix blocking; independent singleton pooling followed by globally serial components with priority drain/deferral; Admission-authorized and executor-proven foreign replacement with identity-neutral exact CAS and record-bound merge-base refresh; and one captured pure scope compatibility query for every relation derivation. The [three-P1 correction trace](design-plan/design.md#three-p1-correction-trace) maps findings to the new contracts and discriminating tests. These are specified corrections, not implementation evidence. Fresh independent review must assess all three together.

The revised canonical spine lint returned green with no errors or warnings (17 anchors); documentation lint with conformance returned ok with no warnings. These mechanical checks validate document structure, not implementation correctness. The reviewer examined all revised members and re-read the final design after the integrator's last semantic edits. Reviewed contract hashes before approval/lifecycle bookkeeping: design.md `5e5456ae14298888113cfafff1f97d74c61cffec80636204999f3bdb7da32a05`, spine.yaml `69ab8b565e4c6bd091802e2ca3a4e32b1425409a7dba62070bae24db147f7481`, ADR `42618f7894d93c5462dd65e7385c9140c7ed9d43c0ef9516e26d13915e03a5e4`. Subsequent edits record approval/lifecycle only, not changes to the reviewed contracts.

Pre-implementation baseline gate: lint, bot reproduction and build passed; coverage reported 1787 passed and two existing investigation RED tests (`/tmp/air-sync-approved-contract-coverage.log`). Production was unchanged at that point, so this is baseline evidence, not verification of the revised implementation. The RED tests use an incomplete destructive-plan seam. Strengthen them into public BatchObservation cases with explicit baseline/entries, preserving the failure counterexample; absent-baseline cases require match→commit→next-cycle no-op. Do not delete or weaken them to obtain a green count. Existing test changes are preserved.

Independent public-entry reproduction also shows remote rename+edit admitted as ordinary rename_local without a transfer instruction. This is an instruction-generation defect observation, not demonstrated filesystem data loss. CAS target overwrite and tracker ABA now have failing-before/passing-after unit tests; they are not established causes of the logged vault error.

Required delivery gate: `npm run lint && npm run lint:bot-repro && npm run build && npm run test:coverage`. Add pure public Admission matrices; execute exact admitted actions against existing filesystem fakes and real test IDB store; inject interruption after every effect/proof/publication; test finalization/ABA/UI and optional checkpoint paths. Specifically require all six B pull/match/conflict → A match/conflict combinations; each prefix failure blocks its suffix; serial entry drains already-running priority and pool work and defers new priority; no overlap is permitted. Test foreign destination policy-required preservation and exact replacement, concurrent source/destination record protection, incompatible merge-base invalidation and late refresh CAS, plus I/O success/CAS failure followed by ordinary re-observation. Test identical included identity facts with differing captured scope eligibility across report/alias/actual/baseline derivations through the single pure query. Preserve scope XOR and both-endpoints-excluded fixtures without an all-physical-subtree scan or excluded identity metadata.

Final user-visible acceptance requires matching deployed build hash plus actual Obsidian file/folder renames, both casing directions and edit/no-edit, followed by repeated clean cycles. Unit tests or a proposed design do not demonstrate that the current vault is repaired.
